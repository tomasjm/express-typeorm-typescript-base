let config = {
  JWT_SECRET: "JWT SECRET",
  oauth: {
    googLe: {
      clientID: "id",
      clientSecret: "secret"
    },
    facebook: {
      clientID: "id",
      clientSecret: "secret"
    }
  }
};
export default config;
